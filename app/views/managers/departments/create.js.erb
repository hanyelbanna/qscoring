$("ul.errors").html("")
<% if @department.errors.any? %>
  <% @department.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"));
  <% end %>
  	$("#modal_place").replaceWith("<%= j(render 'layouts/ajax_error_modal', messages: @department.errors) %>");
    $('#myModal').modal()
<% else %>
$("#data-table").append("<%= j(render @department) %>");
$('#data-form').slideUp(350);
<% end %>