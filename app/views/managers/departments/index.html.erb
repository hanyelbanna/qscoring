<div class="page-head">
  <div class="page-title">
    <h1>Departments <small>All Departments</small></h1>
  </div>
  <div class="page-toolbar">
    <%= link_to new_managers_department_path, remote: true do %>
    <button class="btn blue"><i class="fa fa-plus-circle"></i> New Department</button>
    <% end %>
  </div>
</div>
<%= render partial: "layouts/breadcrumb" %>
<%= render partial: "layouts/portlet_header", locals: {header_color: "red", header_rows: "8", header_title: "Edit Departments"} %>
<div class="table-responsive">
<%= render partial: "layouts/pagination", locals: {pages_collection: @departments} %>
<%= render partial: "layouts/search", locals: {this_page: "managers_departments"} %>



          <table class="table table-striped table-bordered table-hover">
          <thead>
          <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Edit</th>
            <th>Delete</th>
          </tr>
          </thead>
          <tbody id="data-table">
            <%= render @departments %>
          </tbody>
          </table>

<%= render partial: "layouts/pagination", locals: {pages_collection: @departments} %>
<%= render partial: "layouts/portlet_form", locals: {header_color: "purple", header_title: "Department Form"} %>
